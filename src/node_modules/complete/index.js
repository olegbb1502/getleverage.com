var domEvent = require('dom-events')

module.exports = function () {
  var $iframe = document.querySelector('iframe')
  var dest = 'https://calendly.com/getleverage/onboarding'

  $iframe.src = [dest, window.location.search].join('')
}
